fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1
starlette==0.47.3

# HTTP and networking
httpx==0.28.1
httpcore==1.0.9
anyio==4.10.0
h11==0.16.0
sniffio==1.3.1

# Database and ORM
SQLAlchemy==2.0.43
PyMySQL[rsa]==1.1.0
cryptography==41.0.7
# Remove mysql-connector-python to avoid conflicts

# AI and ML (RAG system)
openai==1.107.1
langchain>=0.2
langchain-text-splitters==0.3.11
langchain-qdrant>=0.1.0
langchain-huggingface>=0.1.0
langchain-community>=0.3.0
qdrant-client>=1.7
sentence-transformers==5.1.0
transformers==4.56.1
torch==2.8.0
numpy==2.3.3
tokenizers==0.22.0
huggingface-hub==0.34.4
safetensors==0.6.2

# Utilities
python-dotenv==1.1.1
orjson==3.11.3
tenacity==9.1.2
watchfiles==1.1.0

# Optional dependencies (can be removed if not needed)
# kubernetes==33.1.0  # Only if using Kubernetes features
# posthog==5.4.0     # Only if using analytics
# rich==14.1.0       # Only if using rich console output
# typer==0.17.4      # Only if using CLI tools